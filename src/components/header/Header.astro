---
import TopBanner from './TopBanner.astro';
import { ShoppingBag, Search, Phone, Menu, X } from '@lucide/astro';

const navItems = [
  { name: 'ACTIVITIES', url: 'https://www.robbies.com/' },
  { name: 'FISHING', url: 'https://www.robbies.com/islamorada-fishing.htm' },
  { name: 'RESTAURANT', url: 'https://www.robbies.com/' },
  { name: 'SHOP', url: 'https://shop.robbies.com/' },
  { name: 'OPEN-AIR SHOPPING', url: 'https://www.robbies.com/' },
  { name: 'WEBCAM', url: 'https://www.robbies.com/' },
  { name: 'CONTACT US', url: 'https://www.robbies.com/' },
  { name: "VISITOR'S CENTER", url: 'https://www.robbies.com/' },
];
---

<!-- Header Contenedor Fijo / Sticky (Permanente al hacer scroll) -->
<header id="main-header" class="w-full sticky top-0 z-50 shadow-md font-heading transition-all duration-300">
  <!-- Top Blue Banner Rotativo -->
  <TopBanner />

  <!-- Main Royal Navy Navbar (Color exacto Robbie's #2B447A) -->
  <nav class="bg-[#2B447A] text-white py-2.5 px-3 sm:px-6 lg:px-8 border-b border-blue-900/30">
    <div class="max-w-[1440px] mx-auto flex items-center justify-between gap-2 lg:gap-4">
      
      <!-- Brand Logo -->
      <a href="/" class="flex-shrink-0 flex items-center group transition-transform duration-200 hover:scale-[1.01]">
        <img 
          src="/assets/common/robbies_logo_mobile.webp" 
          alt="Robbie's of Islamorada" 
          class="h-10 sm:h-12 md:h-14 lg:h-16 w-auto object-contain drop-shadow-sm"
          loading="eager"
        />
      </a>

      <!-- Desktop Navigation Menu (Pixel-Perfect spacing & pipe separators) -->
      <div class="hidden xl:flex items-center justify-center flex-1 mx-2 lg:mx-4">
        <ul class="flex items-center flex-wrap justify-center text-[11px] lg:text-[12px] xl:text-[12.5px] font-extrabold tracking-wider text-white">
          {navItems.map((item, index) => (
            <li class="flex items-center">
              <a 
                href={item.url} 
                class="hover:text-amber-300 transition-colors duration-200 py-1 px-1.5 lg:px-2 uppercase whitespace-nowrap"
              >
                {item.name}
              </a>
              {index < navItems.length - 1 && (
                <span class="text-white/40 font-light px-1 select-none text-[10px]">|</span>
              )}
            </li>
          ))}
        </ul>
      </div>

      <!-- Right Action Items (Shop Bag, Search, Phone) -->
      <div class="hidden md:flex items-center space-x-3 lg:space-x-5 text-white flex-shrink-0">
        <!-- Shop Icon -->
        <a 
          href="https://shop.robbies.com/" 
          target="_blank"
          aria-label="Shop Robbie's Merch"
          class="hover:text-amber-300 transition-colors duration-200 p-1.5 rounded-full hover:bg-white/10"
          title="Shop Merch"
        >
          <ShoppingBag class="w-5 h-5 stroke-[2]" />
        </a>

        <!-- Search Icon (triggers search overlay) -->
        <button 
          id="search-trigger-btn"
          aria-label="Search Robbie's site"
          class="hover:text-amber-300 transition-colors duration-200 p-1.5 rounded-full hover:bg-white/10 focus:outline-none"
          title="Search"
        >
          <Search class="w-5 h-5 stroke-[2]" />
        </button>

        <!-- Phone Contact (Pixel-perfect exact replica from image) -->
        <a 
          href="tel:3056648070" 
          class="flex items-center space-x-2 text-white hover:text-amber-300 transition-colors duration-200 font-bold text-xs lg:text-sm tracking-wide ml-1"
        >
          <Phone class="w-4 h-4 fill-white stroke-none flex-shrink-0" />
          <span class="whitespace-nowrap">(305) 664-8070</span>
        </a>
      </div>

      <!-- Mobile Menu Toggle Button -->
      <div class="flex xl:hidden items-center space-x-2 sm:space-x-3">
        <a 
          href="tel:3056648070" 
          class="md:hidden flex items-center space-x-1.5 text-xs font-bold text-white bg-white/10 px-2.5 py-1.5 rounded-full border border-white/20"
        >
          <Phone class="w-3.5 h-3.5 fill-white stroke-none" />
          <span>(305) 664-8070</span>
        </a>

        <button 
          id="mobile-menu-toggle"
          aria-label="Toggle Navigation Menu"
          class="p-1.5 rounded-lg text-white hover:bg-white/10 transition-colors focus:outline-none"
        >
          <Menu id="menu-icon-open" class="w-7 h-7" />
          <X id="menu-icon-close" class="w-7 h-7 hidden" />
        </button>
      </div>

    </div>
  </nav>

  <!-- Mobile Drawer Overlay -->
  <div 
    id="mobile-menu-drawer"
    class="fixed inset-x-0 top-[90px] sm:top-[100px] bottom-0 bg-[#2B447A]/98 backdrop-blur-xl z-50 transform translate-x-full transition-transform duration-300 ease-in-out flex flex-col p-6 overflow-y-auto border-t border-white/10 xl:hidden"
  >
    <div class="flex flex-col space-y-3 my-auto">
      {navItems.map((item) => (
        <a 
          href={item.url} 
          class="text-white hover:text-amber-300 text-base sm:text-lg font-extrabold tracking-wider py-2.5 border-b border-white/10 uppercase transition-colors"
        >
          {item.name}
        </a>
      ))}

      <div class="pt-6 flex flex-col gap-3">
        <a 
          href="https://shop.robbies.com/" 
          target="_blank"
          class="w-full py-3 bg-amber-500 hover:bg-amber-400 text-slate-950 font-bold rounded-xl text-center flex items-center justify-center space-x-2 shadow-md transition-colors text-sm"
        >
          <ShoppingBag class="w-4 h-4" />
          <span>Shop Robbie's Merch</span>
        </a>

        <a 
          href="tel:3056648070" 
          class="w-full py-3 bg-white/15 hover:bg-white/25 text-white font-bold rounded-xl text-center flex items-center justify-center space-x-2 border border-white/20 transition-colors text-sm"
        >
          <Phone class="w-4 h-4 fill-white stroke-none" />
          <span>Call (305) 664-8070</span>
        </a>
      </div>
    </div>
  </div>

  <!-- Search Modal Overlay -->
  <div 
    id="search-modal"
    class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm z-50 hidden items-center justify-center p-4"
  >
    <div class="bg-[#2B447A] border border-white/20 rounded-2xl p-6 w-full max-w-lg shadow-2xl relative">
      <button 
        id="close-search-btn"
        class="absolute top-4 right-4 text-slate-300 hover:text-white p-1"
        aria-label="Close search"
      >
        <X class="w-6 h-6" />
      </button>

      <h3 class="text-white text-lg font-bold mb-4 font-heading">Search Robbie's of Islamorada</h3>
      <form action="https://www.google.com/search" method="get" target="_blank" class="flex gap-2">
        <input 
          type="hidden" 
          name="q" 
          value="site:robbies.com" 
        />
        <input 
          type="text" 
          name="q"
          placeholder="What are you looking for?"
          class="flex-1 bg-white/10 text-white placeholder-slate-300 px-4 py-2.5 rounded-xl border border-white/20 focus:outline-none focus:border-amber-400 text-sm"
          required
        />
        <button 
          type="submit"
          class="bg-amber-500 hover:bg-amber-400 text-slate-950 font-bold px-5 py-2.5 rounded-xl transition-colors flex items-center space-x-1 text-sm"
        >
          <Search class="w-4 h-4" />
          <span>Search</span>
        </button>
      </form>
    </div>
  </div>
</header>

<script>
  const toggleBtn = document.getElementById('mobile-menu-toggle');
  const drawer = document.getElementById('mobile-menu-drawer');
  const iconOpen = document.getElementById('menu-icon-open');
  const iconClose = document.getElementById('menu-icon-close');

  toggleBtn?.addEventListener('click', () => {
    const isOpen = !drawer?.classList.contains('translate-x-full');
    if (isOpen) {
      drawer?.classList.add('translate-x-full');
      iconOpen?.classList.remove('hidden');
      iconClose?.classList.add('hidden');
      document.body.style.overflow = 'auto';
    } else {
      drawer?.classList.remove('translate-x-full');
      iconOpen?.classList.add('hidden');
      iconClose?.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
    }
  });

  // Search Modal
  const searchTrigger = document.getElementById('search-trigger-btn');
  const searchModal = document.getElementById('search-modal');
  const closeSearch = document.getElementById('close-search-btn');

  searchTrigger?.addEventListener('click', () => {
    searchModal?.classList.remove('hidden');
    searchModal?.classList.add('flex');
  });

  closeSearch?.addEventListener('click', () => {
    searchModal?.classList.add('hidden');
    searchModal?.classList.remove('flex');
  });
</script>
